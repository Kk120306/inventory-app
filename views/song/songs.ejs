<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Songs</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>🎵 Songs Dashboard</h1>
    </header>

    <main>
        <section>
            <h2>All Songs</h2>
            <% if (songs.length === 0) { %>
                <p>No songs found.</p>
            <% } else { %>
                <ul>
                    <% songs.forEach(song => { %>
                        <li><strong><%= song.title %></strong> (ID: <%= song.id %>)</li>
                    <% }); %>
                </ul>
            <% } %>
        </section>
    </main>
</body>
</html>
