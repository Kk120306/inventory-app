<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Artists</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>🎵 Artists Dashboard</h1>
    </header>

    <main>
        <section>
            <h2>All Artists</h2>
            <% if (artists.length === 0) { %>
                <p>No songs found.</p>
            <% } else { %>
                <ul>
                    <% artists.forEach(artist => { %>
                        <li><strong><%= artist.name %></strong> (ID: <%= artist.id %>)</li>
                    <% }); %>
                </ul>
            <% } %>
        </section>
    </main>
</body>
</html>
