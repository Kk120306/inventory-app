<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Genres</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <header>
        <h1>🎵 Genres Dashboard</h1>
    </header>

    <main>
        <section>
            <h2>All Genres</h2>
            <% if (genres.length === 0) { %>
                <p>No songs found.</p>
            <% } else { %>
                <ul>
                    <% genres.forEach(genre => { %>
                        <li><strong><%= genre.name %></strong> (ID: <%= genre.id %>)</li>
                    <% }); %>
                </ul>
            <% } %>
        </section>
    </main>
</body>
</html>
